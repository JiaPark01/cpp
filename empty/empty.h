#ifndef EMPTY_H
#define EMPTY_H

class Empty {
public:
	// Empty();		// 다른 생성자가 없을 때 생기는 게 기본 생성자
	// Empty(const Empty& );
	//~Empty();
	
	// Empty& operator=(const Empty& );
	
	// Empty *Empty::operator&();		// pointer형 주소값 반환
	// const Empty *operator&() const;	// 상수 객체를 위한 read-only pointer
};

#endif
